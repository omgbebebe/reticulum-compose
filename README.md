# TL;DR;

Run services with:

```shell
docker compose up
```

After some time you can open webUI with a browser:
- http://localhost:8700 :: MeshChat
- http://localhost:8702 :: RRC web client

All configs and persistent data stored on host in `./data` directory.
